# Stupidly simple auth0 + angular + .net core app

## Setup
Add keys and domain info to `Startup.cs` and `app.module.ts`

## How to run
1. Open sln in favorite editor. Run project
1. Inside ClientApp run `npm start`